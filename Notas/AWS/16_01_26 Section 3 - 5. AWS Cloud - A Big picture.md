16/01/26 Section 3 - 5. AWS Cloud - A Big picture
Computação em núvem é a entrega sobe demanda de recursos de TI pela internet com o preço “pay-as-you-go”

AWS region & availability zones - most important

Region (multiple AZs) → AZs (data centers)

Region Services (DynamoDB, S3)

build an architecture for simplified version of a social media application
	1- Private Data Center → Private Network
	2- Users → IP → Virtual Machine

######  Three Tier Architecture
Web Server → App Server → Relational DB

###### Scaling
Vertical Scaling - More hardware capacity
Horizontally Scaling = More servers and distribution the load across those servers

Load Balancer - Use only one IP for multiple servers

DNS service - translate your domain name to the IP address

DNS → Load Balancer

cache database - stores information locally

NoSQL databases allows you to store different types of the data in to the same database

Graph Database

CDN - Content Delivery Network & Cache, use for distribution storage for distant users

Clickstream analytics